# 💀 HYPER PERSONALISATION N-ORGAN DEPENDENCIES 💀

# FastAPI + ASGI server (compatible with existing environment)
fastapi>=0.100.0
uvicorn[standard]>=0.31.0

# HTTP client for AI API calls (compatible with mcp, grimoire)
httpx>=0.27.0

# Python 3.11+ compatibility
typing-extensions>=4.7.0

# Note: Using minimum version constraints for compatibility
# These versions work with existing dragohan-grimoire and mcp packages
